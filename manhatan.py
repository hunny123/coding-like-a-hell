def manhattondistance(x,y):
    sort(x)
    sort(y)
    i=1
    sumx=[0]*len(x)
    sumy=[0]*len(y)
    while i<len(x):
        sumx[1]=sumx[i-1]+x[i]
        

        
        
        
    
